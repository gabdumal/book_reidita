---
interface Props {
  keys: string[];
}

const { keys } = Astro.props;
---

{
  keys.reduce((acc, key, index) => {
    if (index > 0) {
      acc.push(<span class=""> + </span>);
    }
    acc.push(
      <kbd style="background-color: var(--sl-color-bg-inline-code);" class="font-mono px-1 py-0.5 rounded-sm">{key}</kbd>,
    );
    return acc;
  }, [])
}
